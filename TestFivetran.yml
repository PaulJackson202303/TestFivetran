TestFivetran:
  outputs:

    dev:
      type: postgres
      threads: [1 or more]
      host: [defay.hd.free.fr]
      port: [5432]
      user: [replicate]
      pass: [replicate]
      dbname: [Target]
      schema: [transformed]

    prod:
      type: postgres
      threads: [1 or more]
      host: [host]
      port: [port]
      user: [prod_username]
      pass: [prod_password]
      dbname: [dbname]
      schema: [prod_schema]

  target: dev
